import { Outlet } from "react-router";

function Layout() {
    return ( 
        <>
            <div>
                
            </div>
            <Outlet />
        </>
     );
}

export default Layout;